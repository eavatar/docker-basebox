machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - make

test:
  override:
    - docker run -d --name basebox eavatar/basebox; sleep 10
    - docker exec basebox ps -ef

